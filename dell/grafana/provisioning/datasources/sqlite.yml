apiVersion: 1

datasources:
  - name: Incident SQLite
    uid: incident-sqlite
    type: frser-sqlite-datasource
    access: proxy
    editable: true
    jsonData:
      path: /var/lib/grafana/incidents/incidents.db
